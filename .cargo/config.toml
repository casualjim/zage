# cargo-zigbuild uses Zig as the cross toolchain, so we don't configure
# GCC-based linkers/AR here.

# Configure environment variables for cross-compilation with OpenSSL
[env]
PKG_CONFIG_ALLOW_CROSS = "1"
OPENSSL_STATIC = "1"
