#!/usr/bin/env bash

#MISE description="Build the application (Release)"
#MISE sources=["Cargo.toml", "src/**/*.rs"]
#MISE outputs=["target/debug/context"]
#MISE depends=["test"]

echo "🚀 Starting Frontend release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build --release
